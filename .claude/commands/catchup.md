Read the last 20 git commits with `git log --oneline -20`, then read uncommitted changes with `git diff` and `git status`. Summarize:
1. What changed recently (committed work)
2. What is in-progress (uncommitted changes)
3. What likely needs attention next

Be concise. Use a table for commits.
