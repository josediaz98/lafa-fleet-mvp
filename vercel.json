{
  "buildCommand": "cd fleet-intelligence && npm install && npm run build && cp -r dist ../app",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/app", "destination": "/app/index.html" },
    { "source": "/app/:path*", "destination": "/app/index.html" }
  ]
}
